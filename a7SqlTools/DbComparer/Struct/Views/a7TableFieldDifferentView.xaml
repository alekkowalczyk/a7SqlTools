<UserControl x:Class="a7SqlTools.DbComparer.Struct.Views.a7TableFieldDifferentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Label Padding="5" Margin="5" Content="{Binding Text}"></Label>
            <Button Padding="5" Margin="5" Content="{Binding ButtonCaption}" Command="{Binding ButtonClick}"></Button>
            <Button Padding="5" Margin="5" Content="{Binding Button2Caption}" Command="{Binding Button2Click}" Visibility="{Binding Button2Visibility}"></Button>
        </StackPanel>
    </Grid>
</UserControl>
